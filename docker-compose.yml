# Docker Compose for Local Testing
# This is optional - for testing the Docker build locally before deploying to Render

version: '3.8'

services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Host=db.edxjeuoutitcdfuqzyxp.supabase.co;Database=postgres;Username=postgres;Password=Deji1@Akoms!;SSL Mode=Require;Trust Server Certificate=true
      - JwtSettings__SecretKey=your-super-secret-jwt-key-change-this-in-production
      - JwtSettings__Issuer=ChurchRosterApi
      - JwtSettings__Audience=ChurchRosterClient
      - JwtSettings__ExpirationInMinutes=1440
    networks:
      - church-roster-network

networks:
  church-roster-network:
    driver: bridge
